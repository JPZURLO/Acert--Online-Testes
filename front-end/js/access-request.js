const requestForm=document.getElementById('access-request-form');const requestMessage=document.getElementById('request-message');document.getElementById('request-phone').addEventListener('input',event=>{const digits=event.target.value.replace(/\D/g,'').slice(0,11);event.target.value=digits.length>10?digits.replace(/(\d{2})(\d{5})(\d{4})/,'($1) $2-$3'):digits.replace(/(\d{2})(\d{4})(\d{0,4})/,'($1) $2-$3').replace(/-$/,'')});document.getElementById('request-cnpj').addEventListener('input',event=>{const digits=event.target.value.replace(/\D/g,'').slice(0,14);event.target.value=digits.replace(/(\d{2})(\d{3})(\d{3})(\d{4})(\d{0,2})/,'$1.$2.$3/$4-$5').replace(/-$/,'')});requestForm.addEventListener('submit',async event=>{event.preventDefault();const button=requestForm.querySelector('[type="submit"]');requestMessage.textContent='';button.disabled=true;button.textContent='Enviando...';try{const response=await fetch('/api/access-requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contactName:document.getElementById('request-contact-name').value,companyName:document.getElementById('request-company-name').value,email:document.getElementById('request-email').value,phone:document.getElementById('request-phone').value,cnpj:document.getElementById('request-cnpj').value,planInterest:document.getElementById('request-plan').value,needs:document.getElementById('request-needs').value})});const data=await response.json();if(!response.ok)throw new Error(data.message||'Não foi possível enviar a solicitação.');requestForm.hidden=true;document.getElementById('request-success').hidden=false}catch(error){requestMessage.textContent=error.message||'Não foi possível conectar ao servidor.'}finally{button.disabled=false;button.innerHTML='Enviar solicitação <i class="fa-solid fa-arrow-right"></i>'}});
