const adminLoginForm=document.getElementById('admin-login-form');const adminError=document.getElementById('admin-login-error');document.getElementById('toggle-admin-password').addEventListener('click',()=>{const field=document.getElementById('admin-password');field.type=field.type==='password'?'text':'password'});adminLoginForm.addEventListener('submit',async event=>{event.preventDefault();const button=adminLoginForm.querySelector('[type="submit"]');adminError.textContent='';button.disabled=true;button.textContent='Entrando...';try{const response=await fetch('/login_admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:document.getElementById('admin-email').value.trim(),senha:document.getElementById('admin-password').value})});const type=response.headers.get('content-type')||'';if(!type.includes('application/json'))throw new Error('O servidor administrativo não respondeu corretamente.');const data=await response.json();if(!response.ok)throw new Error(data.message||'Não foi possível entrar.');sessionStorage.setItem('acert-admin-name',data.AdminName||'Administrador');window.location.replace('Admin.html')}catch(error){adminError.textContent=error.message||'Não foi possível conectar ao servidor.'}finally{button.disabled=false;button.textContent='Entrar no painel'}});
